import 'package:flutter/cupertino.dart';

class NerwSDFsf extends StatelessWidget {
  const NerwSDFsf({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("this is hard to learn"),
    );
  }
}